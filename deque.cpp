#include <iostream>
#include <string>
#include <deque>

using namespace std;

int main () {
    int testCase;
    string command,member;
    cin >> testCase;

    for(int i = 0; i < testCase; i++){
        cin >> command;
        if(command == "push_back"){
            cin >> member;
            
        }
    }
}